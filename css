/* style.css */
.container {
    text-align: center;
    margin-top: 50px;
}

h1 {
    font-size: 36px;
    margin-bottom: 20px;
}

table {
    border-collapse: collapse;
    margin: auto;
}

table td {
    width: 100px;
    height: 100px;
    border: 1px solid black;
    cursor: pointer;
    font-size: 36px;
}

button {
    margin-top: 20px;
    padding: 10px 20px;
    font-size: 18px;
    cursor: pointer;
}
